# Focus Timer — Pomodoro Web App

- 25-minute focus sessions with 5-minute breaks
- Clean UI + ambient WebGL background
- Stats persisted in localStorage, exportable as .txt
- Self-contained: single HTML + single JS file
- Run: `python3 -m http.server 8080` then open `http://localhost:8080`
